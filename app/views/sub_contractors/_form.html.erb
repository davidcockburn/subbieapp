<%= form_for(sub_contractor) do |f| %>
  <% if sub_contractor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sub_contractor.errors.count, "error") %> prohibited this sub_contractor from being saved:</h2>

      <ul>
      <% sub_contractor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
